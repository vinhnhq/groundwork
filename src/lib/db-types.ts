/**
 * The Kysely database schema. Table interfaces are added per migration (F5+),
 * hand-maintained here so integration tests catch drift.
 */
// biome-ignore lint/complexity/noBannedTypes: tables land per migration (F5+)
export type DB = {};
