node_modules/
.next/
out/
build/
dist/
coverage/
.env
.env.local
.env*.local
# Vercel env fill-ins hold real secrets once pasted; only the .example is tracked.
deploy/*.env
!deploy/vercel.env.example
*.log
.DS_Store
.turbo/
playwright-report/
test-results/

# tsc incremental build cache — local artifact, not source.
tsconfig.tsbuildinfo

# Agent worktrees (ship-review QA runs) — never part of the repo.
.claude/worktrees/
.vercel
