---
id: <prefix>-doc-glossary
kind: doc
title: Glossary
description: What each domain word means here, and which ones are overloaded.
status: active
updated: YYYY-MM-DD
---


# Glossary

<!--
SEEDED by @vinhnnn/dev-workflow — yours to own. `sync` never overwrites this file.

An empty glossary is worse than none: it says the vocabulary is agreed when it isn't.
Fill it or delete it.

WHY IT EXISTS: a reader only opens a glossary when they know they are confused. The dangerous case
is confident wrongness — seeing a familiar word, assuming the usual meaning, and writing something
coherent and wrong. That is why §Overloaded below is duplicated into CLAUDE.md: those entries have
to arrive before anyone knows to look them up.
-->

## Overloaded terms — copy these into CLAUDE.md

The words that mean something _different here_ than elsewhere in the codebase or the industry. Keep
this list short enough to paste into the rule file; if it grows past ~6 entries, the naming is the
problem, not the glossary.

| Term | Here it means | It does **not** mean |
| ---- | ------------- | -------------------- |
|      |               |                      |

## Domain vocabulary

One line each. The test of a good entry: a new team member could use the word correctly in a
sentence after reading it.

| Term | Meaning | Lives in |
| ---- | ------- | -------- |
|      |         |          |

## Deliberately not used

Words the team has decided against, and what to say instead. This section prevents the slow drift
where three synonyms end up in the codebase for one concept.

| Avoid | Use instead | Why |
| ----- | ----------- | --- |
|       |             |     |
